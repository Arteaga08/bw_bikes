{
  "name": "@bw-bikes/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"(e2e) Playwright suite — run via 'pnpm test:e2e', not 'pnpm -r test'\"",
    "test:e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@bw-bikes/shared": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.7.0",
    "dotenv": "^16.4.5",
    "eslint": "^9.11.1",
    "otplib": "^13.4.1",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.8.0"
  }
}

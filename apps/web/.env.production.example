# Set these in the hosting provider's secret manager. Never commit real values.

# Server-only — points at the real API host (see next.config.ts's rewrites).
# NOT prefixed NEXT_PUBLIC_: the browser never talks to this URL directly.
API_URL=https://api.blackandwhitebikes.com

# Server-only — same value as apps/api's CLOUDINARY_CLOUD_NAME.
CLOUDINARY_CLOUD_NAME=
